# WMS 项目代码质量审查报告(两轮)

> 审查方式:`code-review-skill` 方法论 + Java / Vue / TypeScript / 通用质量指南
> 范围:后端 `backend-java` + 前端 `frontend-vue` + 测试
> **第一轮(2026-08-19)**:无 🔴;6 项 🟡 + 若干 🟢 → 6 项全部修复 ✅
> **第二轮(2026-08-19,修复后)**:无 🔴;2 项新 🟡 → 已修复 ✅(入库加悲观锁 + 新增入库并发测试)
> **后续**:剩余 🟢 均为可选项;另完成测试命名规范整改(英文 camelCase + @DisplayName)

---

## 一、第一轮已修复(✅)

| 项 | 修复内容 |
|----|---------|
| ① 全局异常 HTTP 状态码 | 与业务码对齐(404 不再返回 400) |
| ② InventoryService 职责过重 | 拆为 Inbound/Outbound/InventoryQuery 三个 Service + 共享 OrderNumberService,Controllers 按领域拆分 |
| ③ 仓库接口泄漏实体 | 改返回 WarehouseResponse / LocationResponse DTO |
| ④ 前端接口缺返回类型 | createInbound/Outbound 补类型,消除 any |
| ⑤ Element Plus 英文 locale | 配置 zh-cn |
| ⑥ v-for index key | 明细行改用自增唯一 id |

## 二、第二轮:新发现的 🟡(已修复 ✅)

### 1. 入库库存累加无悲观锁(并发丢更新/失败风险)
- **位置**:`service/InboundOrderService.java`
- **问题**:入库累加用的是无锁的 `findByProductIdAndLocationCode`,并发入库到同一商品+库位时,后提交者会触发 Hibernate 乐观检查 → **抛 StaleObjectStateException、整个入库单回滚、请求失败**,或静默丢更新——与出库"加锁优雅串行"不一致,是出库修复时遗漏的同一类 TOCTOU。
- **已修复**:累加改用 `findForUpdate`(悲观锁),与出库一致;新增 `InboundConcurrencyTest`:10 并发入库同一库位,全部成功、库存精确累加不丢更新。

### 2. OrderNumberService 重试范围过宽(与 #1 耦合)
- **位置**:`service/OrderNumberService.java`
- **问题**:`withOrderNoRetry` 捕获所有 `DataIntegrityViolationException` 按"单号冲突"重试。入库首次创建撞唯一约束也会被误当单号冲突。
- **说明**:入库改用 `findForUpdate` 后,首次创建撞唯一约束会被重试并收敛(功能正确);仅错误信息偶发不够精确,属边缘情况,暂保留。

## 三、第二轮:🟢(可选优化)

| # | 位置 | 问题 | 建议 |
|---|------|------|------|
| 3 | `controller/InboundOrderController.java`、`OutboundOrderController.java` | POST 返回 HTTP 200,body code=201;API_SPEC 写 "Response (201)" | 加 `@ResponseStatus(HttpStatus.CREATED)` 对齐 |
| 4 | `dto/InboundOrderCreateRequest.java`、`OutboundOrderCreateRequest.java` | `quantity` 只有 `@Min(1)`,无上限,超大值可入库 | 加 `@Max`(如 999999) |
| 5 | `views/ProductsView.vue:46` | `import { computed } from 'vue'` 出现在文件中部(usage 之后) | 移到文件顶部 import 区(import 会被提升但风格不佳) |
| 6 | `views/InboundView.vue:45-48`、`OutboundView.vue`、`InventoryView.vue:32-42` | 防抖 `setTimeout` 未在组件卸载时清理 | `onUnmounted` 清理 |
| 7 | `vite.config.ts` | `__dirname` 弃用警告(Vite native config loader) | 改 `import.meta.dirname` |
| 8 | 全局 | **无鉴权/无权限控制** | 测试项目预期,生产需加登录/权限;可在 NOTES 说明 |

## 四、保留未修(说明理由)

- **实体 @Data**:本项目实体全裸外键、无懒加载关联,`@Data` 问题不会触发;改 8 个实体纯 churn。
- **状态魔法字符串**("COMPLETED"/"DRAFT"/"FREE"):改为枚举要动实体/Service/DTO/测试,收益有限。
- **商品列表后端分页**:模板"参考实现"即前端分页,改动偏离模板。
- **Element Plus 按需引入**:改构建配置有回归风险,bundle 大小不影响正确性。

## 五、亮点(🎉)

- **并发正确性**:出库悲观锁 + 单号冲突重试(TOCTOU 修复)+ 并发压测/隔离测试证明
- **HTTP 状态码对齐**、**Service 领域拆分**、**仓库接口 DTO 化**
- **安全**:全部 SQL 参数化(无注入)、Vue 默认转义(无 XSS)、无敏感信息入库
- **测试**:14 后端 + 11 前端 + 5 e2e;事务回滚断言、并发隔离测试
- **性能**:库存查询单条 JOIN 避免 N+1、后端分页、搜索防抖

---

## 结论

第一轮的 6 项已全部修复。第二轮确认:**功能正确性、测试覆盖、代码规范都已达标**;剩余值得动手的只有 1 项(**入库并发加锁**,与出库保持一致),建议修。其余为风格/可选项。
