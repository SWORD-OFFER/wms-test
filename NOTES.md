# NOTES.md — 开发过程与 AI 使用说明

> 技术栈:后端 **Java 17 + Spring Boot 3** / 前端 **Vue 3 + Element Plus**
> 完成情况:**必做任务 1/2/3 全部完成,选做 A(出库+并发)/B(单元测试)/C(前端性能)全部完成**
> 测试规模:**后端 20 个 + 前端 11 个 + Playwright 端到端 5 个,全部通过**

---

## 1. 使用的 AI 工具及使用方式

工具:**Claude Code**(命令行 AI 编程助手)。

我的使用方式不是"让 AI 一次性全量生成",而是**以理解为主、任务逐个推进**,整体走的就是需求文档推荐的工作流:**需求理解 → 与 AI 讨论方案 → AI 生成代码 → 审查修改 → 测试验证 → 小步提交**:

1. **先分析,后动手**:通读 `README.md`/`TASKS.md`/`API_SPEC.md`/模板代码,先明确"人家要什么、考核点是什么",再开始写
2. **一个任务一个任务来**:每个功能先和 AI 讨论方案、讲清"为什么这么设计",理解了再实现,实现完测试验证,再进入下一个
3. **把上下文喂给 AI**:把相关实体、DTO、表结构作为上下文,让 AI 生成初版(示例见第 3 节)
4. **逐段审查**:重点查事务边界、并发、空值、异常处理——AI 最容易漏这些
5. **测试验证**:curl + 单元测试 + 并发压测 + 端到端测试,不只靠"能跑"
6. **小步提交**:每个功能点一次 commit,message 有意义,提交历史就是开发过程

> 我的体会:AI 最大的价值是**加速实现和查漏**,但**设计和验证的主动权必须在自己手里**,这样才能保证每一行都能讲清楚为什么。

## 2. AI 协作工作流:每个阶段 AI 做什么、我把什么关

| 阶段 | AI 参与什么 | 我的把关 |
|------|------------|---------|
| 需求分析 | 帮我拆解任务、识别风险(如并发超卖、单号冲突) | 对照 TASKS/API_SPEC 逐条确认考核点,不做过度设计 |
| 设计 | 参与接口设计、表结构讨论(如 JPQL JOIN 还是 N+1) | 由我拍板事务边界与并发方案,再让 AI 落地 |
| 编码 | 生成模板代码、DTO、CRUD 重复代码 | 逐段审查事务/并发/异常,不在没理解前提交 |
| 调试 | 分析日志和异常堆栈、定位问题线索 | 我复现并确认根因(如单号 TOCTOU),再修 |
| 优化 | 用 code-review-skill 做全面 Code Review | 逐条判断哪些该修、怎么修符合原设计 |

关键原则:**AI 负责"快",我负责"对"**。AI 给的初版只是草案,正确性由测试和我的审查兜底。

## 3. AI Prompt 示例(如何把上下文喂给 AI)

需求文档强调 Prompt 要**提供充分上下文**。差的问法是"写一个出库功能";我实际用的是带技术栈、已有结构、约束和交付要求的完整 Prompt。以下是出库功能的真实写法(节选):

```
我正在开发一个 Spring Boot 3 + Spring Data JPA 的库存系统(WMS)。
当前需求:实现"出库单创建"功能。

已有信息:
1. Inventory 实体(productId, locationCode, quantity, updatedAt, 唯一约束(productId, locationCode))
2. OutboundOrder / OutboundOrderItem 表结构(orderNo, customerName, status, ...)
3. 入库已实现,模式可参考 InboundOrderService(事务 + 自动生成单号 IN-YYYYMMDD-XXX)

要求:
1. 库存不能扣成负数(先校验余额再扣减)
2. 必须支持并发请求,不得超卖——请先对比悲观锁与乐观锁在该场景的取舍,再给出实现
3. 使用 @Transactional 保证一致性
4. 失败时抛业务异常,由全局异常处理器统一返回,不要吞异常
5. 请先分析方案,再生成代码
```

Code Review 时用的 Prompt:

```
请从以下角度审查本项目代码:
1. 是否存在事务边界问题
2. 是否存在并发安全隐患
3. 是否存在 SQL 性能问题(N+1、缺少索引)
4. 是否存在异常处理缺陷(吞异常、状态码不一致)
5. 是否符合 Spring Boot / RESTful 最佳实践
按严重程度分「必须修 / 建议改 / 可选优化」,每条给出 文件:行号。
```


## 4. AI 帮我解决的一个具体问题(库存查询 JOIN)

**任务**:库存查询需要一次带出商品名、SKU、仓库名,支持模糊搜索、仓库筛选、分页。JPQL 的 `SELECT NEW dto(...)` 构造表达式(要求 DTO 全参构造器、字段顺序与查询一致)容易踩坑。

AI **帮助我快速定位**了 JPQL JOIN 分页查询的正确写法(含 countQuery),省去了查文档和试错的时间。但我没有直接采用,而是**逐项核验了**:

- DTO 字段映射与查询列是否一一对应、顺序一致
- countQuery 是否与原查询条件完全同步(否则分页总数会错)
- 分页边界与空关键字处理
- 是否真的避免了 N+1(只发一条 JOIN,而不是逐行再查商品/仓库)


## 5. AI 生成代码的问题,我如何发现和修复

**例 1(选做 A 的核心)**:AI 初版生成的库存扣减是"先查库存 → 判断足够 → 再扣减",**没有并发控制**。我推演了超卖场景(库存 100、两个请求同时各出 60,会都判定"够"然后都扣减导致库存为负),改为**悲观锁 `SELECT ... FOR UPDATE`**:事务内锁行、锁内校验余额。并用 30 个并发请求压测:300 件库存各扣 20 → **恰好 15 单成功、15 单被"库存不足"拦截、最终库存 0,无超卖**。

**例 2(单号并发冲突)**:AI 生成的订单号用"当天单数 +1",我以为加了唯一索引就够。写并发测试时发现:真并发下多个请求同时读到相同计数 → 生成相同单号 → 撞唯一约束 → **请求 500**。这是典型的 TOCTOU(先查后用)竞态。修复:用 `saveAndFlush` 让冲突在保存时立即抛出,捕获 `DataIntegrityViolationException` 后**自动重试取下一个序号**(新事务重新计数)。新增隔离测试(每线程不同库存行、只竞争单号)验证:10 并发全成功、0 冲突。

**例 3(删除商品吞异常)**:端到端测试时发现,商品删除接口的 `catch { // 取消 }` 把**接口失败的异常也吞掉了**——后端拒绝删除时用户完全看不到原因。修复为"用户取消"和"接口失败"分开处理,失败时展示后端错误。

## 6. AI 犯错统计:真的审过 AI 输出

| AI 生成内容 | 问题 | 如何发现 | 修复 |
|------------|------|---------|------|
| 库存扣减 | 没考虑并发,会超卖 | 推演 + 并发压测(300 库存 30 并发) | 悲观锁 `FOR UPDATE`,锁内校验 |
| 订单编号 | 存在竞态(TOCTOU),并发撞唯一索引 | 并发测试复现 500 | `saveAndFlush` + 捕获唯一约束冲突自动重试 |
| 商品删除 | `catch` 把接口失败异常也吞掉 | 端到端测试发现用户看不到原因 | "取消"与"失败"分开处理,异常透传 |
| DTO 设计 | 内部类包级私有,跨包/反序列化报错 | 编译失败 | 改为 `public static` 嵌套类 |


## 7. 选做 A 的并发方案及理由(为什么选悲观锁)

- **方案对比**:
  - 悲观锁 `SELECT FOR UPDATE`:锁住库存行,并发扣减串行化,最简单可靠
  - 乐观锁 `version`:冲突时重试;扣库存是写多场景,冲突频繁时重试成本高
  - 条件更新 `UPDATE ... WHERE quantity>=n`:适合纯单表单条扣减,但无法在锁内做多行/业务校验
- **结论**:扣减库存是**写多、冲突频繁**的场景,选悲观锁在正确性和实现成本之间最平衡。关键点:**锁必须在事务内,持有到事务提交才释放**,否则先解锁后扣减仍有超卖窗口。

## 8. 测试设计说明:为什么需要这些测试

需求文档强调"AI 生成代码不能直接提交,需要运行、测试、修 bug"。我的测试分三层,各有目的:

| 测试类型 | 覆盖目标 | 关键用例 |
|---------|---------|---------|
| 后端单元测试(Service) | 业务逻辑正确性 | 入库库存累加、出库扣减、库存不足拦截、商品分页 |
| 后端并发测试 | 并发安全 | 300 库存 30 并发不超卖、10 并发单号 0 冲突、入库并发不丢更新 |
| 前端单元测试(Vitest) | 组件/工具逻辑 | 入库表单校验、库存筛选逻辑、低库存判断 |
| Playwright 端到端 | 真实用户流程 | 商品管理 → 入库 → 库存查询 → 出库 → Bug1 修复点 |

**为什么并发要单独测**:普通单元测试只验证"单线程逻辑正确",而超卖、单号冲突这类 bug 只在真并发下暴露——这也是选做 A 的核心考核点。三层测试合起来,才覆盖了"逻辑对 + 并发安全 + 用户真的能用"。

## 9. 用 AI 审 AI:形成审查闭环

最后我用 `code-review-skill` 对本项目做了全面 Code Review,形成了"**AI 生成 → 人工审 → 再用 AI 审**"的闭环(审查 Prompt 见第 3 节)。审查发现并修复了 6 项问题:

1. `InventoryService` 职责过重 → 拆为 **Inbound/Outbound/InventoryQuery 三个 Service** + 共享 `OrderNumberService`
2. 全局异常 HTTP 状态码与业务码不一致(如商品不存在返回 400)→ **404 对齐 404**
3. 仓库接口泄漏 JPA 实体 → 改为返回 **DTO**
4. 前端接口缺返回类型 → 消除 `any`,补上 TS 类型
5. Element Plus 默认英文 locale → 配置**中文**
6. 明细行 `v-for` 用 index 作 key → 改为**唯一 id**

> 这一步是需求文档推荐工作流中"审查修改"环节的体现:AI 生成 ≠ 可提交,必须经过审查。

## 10. 遇到的问题与解决

1. **模板 DTO 内部类不可用**:`InboundOrderCreateRequest` 明细类是包级私有非 static 内部类,跨包访问不到、Jackson 反序列化也会报错。改为 `public static` 嵌套类。
2. **H2 内存库**:重启数据重置,靠 `DataInitializer` 自动重建示例数据,便于演示。
3. **单号并发冲突**:见第 5 节例 2。附带发现 H2 默认锁超时(1s)在并发下会导致连接层瞬态失败,提至 10s。

## 11. 如果更多时间,我还会做

- **分布式锁**:多实例部署时单库行锁不够,用 Redis `SET NX EX` 分布式锁
- **订单幂等**:防止前端重复点击提交生成重复单(唯一订单号 + 幂等键)
- **入库单状态机**:当前直接 `COMPLETED`,更真实的是 `DRAFT → 审核 → CONFIRMED` 两段式
- **软删除 + 审计日志**:删除商品改为 `deleted` 标记,记录操作人/时间
- **数据库索引**:按查询条件补组合索引,大偏移分页改游标分页
- **前端虚拟滚动**:库存数据 500+ 条时用 `el-table-v2` 只渲染可视区(当前已用后端分页 + 防抖满足选做 C)
- **CI 集成**:GitHub Actions 自动跑 `./mvnw test` + `npm test` + Playwright

## 12. Git 提交记录说明

采用**小步提交**,每个功能点一次提交,message 遵循 `feat(模块): / fix(模块): / test(模块): / refactor(模块):` 规范,提交历史即开发过程(共 23 个提交,可清晰看到 实现 → 测试 → 修复 → 重构 的演进):

```
feat(env): 前端代理指向 Spring Boot 后端，补齐 Maven Wrapper
feat(backend): 实现入库单创建（事务 + 单号生成 + 库存累加）
feat(backend): 实现库存查询（JOIN 避免 N+1 + 分页）
fix(backend): 删除商品前校验关联库存，防止孤立库存数据
feat(frontend): 实现入库单创建表单
feat(frontend): 实现库存查询列表（搜索防抖+低库存高亮+后端分页）
fix(frontend): 编辑商品后保留当前页码
feat(frontend): 实现出库单创建表单，新增路由与菜单
feat(backend): 实现出库单与悲观锁库存扣减（选做A）
test(backend): 入库单创建 service 单元测试（含事务回滚）
test(frontend): 库存筛选逻辑单元测试（Vitest）
docs: 添加 NOTES.md
test(backend): 补充出库/商品删除/库存查询单元测试
test(frontend): 补充入库/库存页组件测试
fix(frontend): 删除商品失败时展示后端错误
test(frontend): 新增 Playwright 端到端测试
test(backend): 出库并发扣减测试
fix(backend): 订单单号并发冲突自动重试
test(backend): 单号并发隔离测试 + 出库并发防超卖测试
docs: NOTES.md 补充单号并发修复记录
refactor(backend): 按领域拆分 InventoryService 与 Controllers
fix(backend): 全局异常 HTTP 状态码对齐 + 仓库返回 DTO
fix(frontend): 接口返回类型 + 中文 locale + 明细行唯一 key
```
