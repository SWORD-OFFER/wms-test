# NOTES.md — 开发过程与 AI 使用说明

> 技术栈:后端 **Java 17 + Spring Boot 3** + 前端 **Vue 3 + Element Plus**
> 完成情况:**必做任务 1/2/3 全部完成,选做 A(出库+并发)/B(单元测试)/C(前端性能)全部完成**

---

## 1. 使用的 AI 工具及使用方式

- 工具:**Claude Code**(命令行 AI 编程助手)
- 工作流(遵循 AI_USAGE_GUIDE 推荐的 6 步):
  1. 先通读 `TASKS.md` / `API_SPEC.md` / 模板代码,理解需求
  2. 把相关实体、DTO、表结构作为上下文喂给 AI,讨论方案
  3. AI 生成初版代码
  4. **逐段审查**:事务边界、并发、空值、异常处理,AI 容易漏的边界条件手动补齐
  5. 用 curl / 单元测试 / 并发压测验证
  6. 每完成一个功能点小步提交 git

## 2. AI 帮我解决的一个具体问题

**库存查询的 JOIN 查询**:需要一次带出商品名、SKU、仓库名,还要支持模糊搜索、仓库筛选、分页。JPQL 的 `SELECT NEW dto(...)` 构造表达式语法(要求 DTO 全参构造器、字段顺序一致)容易踩坑,AI 直接给出了一条正确的 JOIN 分页查询(含 countQuery),省去了查文档和试错时间,并且保证了**避免 N+1**——这正是任务 2 的考核点。

## 3. AI 生成代码的问题,我如何发现和修复

**例 1(选做 A 的核心)**:AI 初版生成的库存扣减逻辑是"先查库存 → 判断足够 → 再扣减",没有并发控制。我推演了超卖场景:库存 100 件,两个请求同时各出 60 件,会都判定"够"然后都扣减,导致库存为负。我修复为**悲观锁 `SELECT ... FOR UPDATE`**:在事务内锁定库存行,锁内校验余额再扣减。并用 30 个并发请求压测验证:300 件库存各扣 20,恰好 15 单成功、15 单被"库存不足"拦截、最终库存为 0,**没有超卖**。

**例 2(任务 3 后端 Bug)**:删除商品接口初版没有校验关联库存。我复现"删除有库存的商品 → 库存查询 JOIN 不出商品名",判断是留下孤立库存数据,修复为删除前用 `existsByProductId` 校验并拒绝。

## 4. 选做 A 的并发方案及理由(为什么选悲观锁)

- **方案对比**:
  - 悲观锁 `SELECT FOR UPDATE`:锁住库存行,并发扣减串行化,最简单可靠
  - 乐观锁 `version`:冲突时重试,扣库存是写多场景,冲突频繁时重试成本高
  - 条件更新 `UPDATE ... WHERE quantity>=n`:适合纯单表单条扣减,但无法在锁内做多行/业务校验
- **结论**:扣减库存是**写多、冲突频繁**的场景,选悲观锁在正确性和实现成本之间最平衡。关键点:**锁必须在事务内,持有到事务提交才释放**,否则先解锁后扣减仍有超卖窗口。

## 5. 遇到的问题与解决

1. **前端代理端口指向 FastAPI**:模板 `vite.config.ts` 的 `/api` 代理指向 `http://localhost:8000`(Python 后端),而我选的是 Java 后端(8080),导致前端调不到后端。改为 8080。
2. **模板 DTO 内部类不可用**:`InboundOrderCreateRequest` 的明细类是包级私有非 static 内部类,Service 在别的包访问不到,且 Jackson 反序列化非静态内部类会报错。改为 `public static` 嵌套类。
3. **H2 内存库**:重启后端数据重置,靠 `DataInitializer` 自动重建示例数据,便于演示。

## 6. 如果更多时间,我还会做

- **分布式锁**:多实例部署时单库行锁不够,用 Redis `SET NX EX` 分布式锁
- **订单幂等**:防止前端重复点击提交生成重复单(用唯一订单号 + 幂等键)
- **入库单状态机**:当前直接 `COMPLETED`,更真实的是 `DRAFT → 审核 → CONFIRMED` 两段式
- **软删除 + 审计日志**:删除商品改为 `deleted` 标记,记录操作人/时间
- **数据库索引**:按查询条件补充组合索引,大偏移分页改游标分页
- **前端虚拟滚动**:库存数据 500+ 条时用 `el-table-v2` 只渲染可视区

## 7. Git 提交记录说明

采用**小步提交**,每个功能点一次提交,message 遵循 `feat(模块): / fix(模块): / test(模块):` 规范,提交历史即开发过程:
```
feat(env): 前端代理指向 Spring Boot 后端，补齐 Maven Wrapper
feat(backend): 实现入库单创建（事务 + 单号生成 + 库存累加）
feat(backend): 实现库存查询（JOIN 避免 N+1 + 分页）
fix(backend): 删除商品前校验关联库存，防止孤立库存数据
feat(frontend): 实现入库单创建表单
feat(frontend): 实现库存查询列表
fix(frontend): 编辑商品后保留当前页码
feat(frontend): 实现出库单创建表单
feat(backend): 实现出库单与悲观锁库存扣减（选做A）
test(backend): 入库单创建 service 单元测试
test(frontend): 库存筛选逻辑单元测试
```
